@page
@model E_Migrant.App.Presentacion.Pages.CrudServicio.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Servicio[0].NombreServicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servicio[0].FechaInicioOferta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servicio[0].FechaFinOferta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Servicio[0].Estado)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Servicio) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreServicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInicioOferta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaFinOferta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Estado)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
